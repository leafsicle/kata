var isSquare = function(n){
  if(n>=0){
    let square = Math.sqrt(n)
    return Math.floor(square)===square
  } else {
    return false;
  }
}
//clever solution from submission:
//const isSquare = n => Number.isInteger(Math.sqrt(n));

